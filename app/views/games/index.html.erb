<h2 class="page-content make-it-green">
  Lobby
  <br/>

  <% @open_games.each do |game| %>
    <br/>
    <%= link_to "Hosted by #{game.user.username} #{game.id}", game_path(game), class: "make-it-green" %>
    <br/>
  <% end %>
</h2>
